[package]
name = "exifmeta"
version = "0.1.0"
edition = "2024"
description = "Read metadata.yaml and write EXIF metadata to image files"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
